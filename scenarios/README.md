# Scenarios

Runnable drivers for the signal/chaos catalog in
docs/research/validation/telemetry-playground-sample-project.md (§11). Implemented:
A1 (checkout distributed trace), A12 (CLI run). The rest (async/broker, GraphQL,
chaos via flagd, deploy-regression, canary redaction) follow as the Java/web
tiers land. Drive comparison by running a scenario, then opening each backend UI.
