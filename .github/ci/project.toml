# Generated by velnor-workflow. Regenerate; do not hand-edit.
schema = 2
repository = "tailrocks/parallax-telemetry-playground"
profile = "generic"
verified = true
default_branch = "main"
runners = "github"

[analysis]
method = "static-filesystem-and-manifest-inspection"
detected = ["cargo-lockfile", "cargo-nextest-policy", "mise-present", "package-manager:bun:web", "package-script:web:build", "package-script:web:test", "package-script:web:typecheck", "renovate-configuration", "rust-binaries:playground-cli:1", "rust-build-script:playground-proto", "rust-test-targets:playground-cli:1", "rust-test-targets:playground-telemetry:1", "rust-workspace-members:10", "swift-package:macos"]
limitations = ["Project code, build scripts, task runners, and commands are never executed during analysis.", "Release, signing, registry, deployment, branch-protection, and runner-capability contracts remain explicit manual inputs.", "Renovate credentials, runner placement, and write permissions cannot be inferred from repository files.", "Rust build scripts are watched but their system dependencies and generated outputs are not inferred."]

[workflow]
github_runner = "ubuntu-24.04"
velnor_labels = ["self-hosted", "velnor-target-mvp"]
files = ["ci-main.yml", "ci-policy.yml", "ci-pr.yml", "ci-unit-bun.yml", "ci-unit-docker.yml", "ci-unit-gradle.yml", "ci-unit-rust.yml", "ci-unit-swift.yml", "maintenance.yml", "nightly.yml"]
notes = ["Rust verification uses Mr. Boxington 1.12.0 with its GitHub cache backend and bounded snapshot keys (a key names the toolchain/image/linker compatibility class and the hashed source state, so a new source state saves a new snapshot and retention keeps generations bounded).", "Swift units render on the default executor: swift-package-macos (no Apple requirement detected; if Apple-bound, declare `capabilities = [\"xcode\"]` or `[\"xcframework\"]` on a [[units]] row, otherwise ensure a Swift toolchain provisions on ubuntu-24.04)."]

[release]
enabled = false
reason = "Release is fail-closed. Enable only after declaring immutable artifact, registry, provenance, and tag-protection policy."

[[unit]]
id = "bun-playground-web"
label = "Bun package (playground-web)"
kind = "bun"
root = "web"
watch = ["web/**/*.js", "web/**/*.json", "web/**/*.jsx", "web/**/*.ts", "web/**/*.tsx", "web/bun.lock", "web/bun.lockb", "web/package-lock.json", "web/package.json"]
github_pr_commands = ["cd -- 'web' && bun install --frozen-lockfile", "cd -- 'web' && bun run typecheck", "cd -- 'web' && bun run build", "cd -- 'web' && bun run test"]
github_full_commands = ["cd -- 'web' && bun install --frozen-lockfile", "cd -- 'web' && bun run typecheck", "cd -- 'web' && bun run build", "cd -- 'web' && bun run test"]
velnor_pr_commands = ["cd -- 'web' && bun install --frozen-lockfile", "cd -- 'web' && bun run typecheck", "cd -- 'web' && bun run build", "cd -- 'web' && bun run test"]
velnor_full_commands = ["cd -- 'web' && bun install --frozen-lockfile", "cd -- 'web' && bun run typecheck", "cd -- 'web' && bun run build", "cd -- 'web' && bun run test"]
[unit.cache]
key_files = ["web/package.json", "web/bun.lock"]
paths = ["~/.bun/install/cache"]

[[unit]]
id = "docker-deploy"
label = "Docker (deploy)"
kind = "docker"
root = "deploy"
watch = ["deploy/**"]
github_pr_commands = ["docker build --file 'deploy/Dockerfile.java' --tag local-ci:deploy-dockerfile-java 'deploy'", "docker build --file 'deploy/Dockerfile.migrations' --tag local-ci:deploy-dockerfile-migrations 'deploy'", "docker build --file 'deploy/Dockerfile.rust' --tag local-ci:deploy-dockerfile-rust 'deploy'", "docker build --file 'deploy/Dockerfile.web' --tag local-ci:deploy-dockerfile-web 'deploy'"]
github_full_commands = ["docker build --file 'deploy/Dockerfile.java' --tag local-ci:deploy-dockerfile-java 'deploy'", "docker build --file 'deploy/Dockerfile.migrations' --tag local-ci:deploy-dockerfile-migrations 'deploy'", "docker build --file 'deploy/Dockerfile.rust' --tag local-ci:deploy-dockerfile-rust 'deploy'", "docker build --file 'deploy/Dockerfile.web' --tag local-ci:deploy-dockerfile-web 'deploy'"]
velnor_pr_commands = ["docker build --file 'deploy/Dockerfile.java' --tag local-ci:deploy-dockerfile-java 'deploy'", "docker build --file 'deploy/Dockerfile.migrations' --tag local-ci:deploy-dockerfile-migrations 'deploy'", "docker build --file 'deploy/Dockerfile.rust' --tag local-ci:deploy-dockerfile-rust 'deploy'", "docker build --file 'deploy/Dockerfile.web' --tag local-ci:deploy-dockerfile-web 'deploy'"]
velnor_full_commands = ["docker build --file 'deploy/Dockerfile.java' --tag local-ci:deploy-dockerfile-java 'deploy'", "docker build --file 'deploy/Dockerfile.migrations' --tag local-ci:deploy-dockerfile-migrations 'deploy'", "docker build --file 'deploy/Dockerfile.rust' --tag local-ci:deploy-dockerfile-rust 'deploy'", "docker build --file 'deploy/Dockerfile.web' --tag local-ci:deploy-dockerfile-web 'deploy'"]

[[unit]]
id = "gradle-services-catalog"
label = "Gradle (services/catalog)"
kind = "gradle"
root = "services/catalog"
watch = ["services/catalog/build.gradle", "services/catalog/build.gradle.kts", "services/catalog/gradle/**", "services/catalog/gradlew", "services/catalog/gradlew.bat", "services/catalog/settings.gradle", "services/catalog/settings.gradle.kts"]
github_pr_commands = ["cd -- 'services/catalog' && gradle check --no-daemon"]
github_full_commands = ["cd -- 'services/catalog' && gradle check --no-daemon"]
velnor_pr_commands = ["cd -- 'services/catalog' && gradle check --no-daemon"]
velnor_full_commands = ["cd -- 'services/catalog' && gradle check --no-daemon"]
[unit.cache]
key_files = ["services/catalog/gradle/wrapper/gradle-wrapper.properties", "services/catalog/gradle/libs.versions.toml"]
paths = ["~/.gradle/caches", "~/.gradle/wrapper"]

[[unit]]
id = "gradle-services-fulfillment"
label = "Gradle (services/fulfillment)"
kind = "gradle"
root = "services/fulfillment"
watch = ["services/fulfillment/build.gradle", "services/fulfillment/build.gradle.kts", "services/fulfillment/gradle/**", "services/fulfillment/gradlew", "services/fulfillment/gradlew.bat", "services/fulfillment/settings.gradle", "services/fulfillment/settings.gradle.kts"]
github_pr_commands = ["cd -- 'services/fulfillment' && gradle check --no-daemon"]
github_full_commands = ["cd -- 'services/fulfillment' && gradle check --no-daemon"]
velnor_pr_commands = ["cd -- 'services/fulfillment' && gradle check --no-daemon"]
velnor_full_commands = ["cd -- 'services/fulfillment' && gradle check --no-daemon"]
[unit.cache]
key_files = ["services/fulfillment/gradle/wrapper/gradle-wrapper.properties", "services/fulfillment/gradle/libs.versions.toml"]
paths = ["~/.gradle/caches", "~/.gradle/wrapper"]

[[unit]]
id = "gradle-services-payment"
label = "Gradle (services/payment)"
kind = "gradle"
root = "services/payment"
watch = ["services/payment/build.gradle", "services/payment/build.gradle.kts", "services/payment/gradle/**", "services/payment/gradlew", "services/payment/gradlew.bat", "services/payment/settings.gradle", "services/payment/settings.gradle.kts"]
github_pr_commands = ["cd -- 'services/payment' && gradle check --no-daemon"]
github_full_commands = ["cd -- 'services/payment' && gradle check --no-daemon"]
velnor_pr_commands = ["cd -- 'services/payment' && gradle check --no-daemon"]
velnor_full_commands = ["cd -- 'services/payment' && gradle check --no-daemon"]
[unit.cache]
key_files = ["services/payment/gradle/wrapper/gradle-wrapper.properties", "services/payment/gradle/libs.versions.toml"]
paths = ["~/.gradle/caches", "~/.gradle/wrapper"]

[[unit]]
id = "rust-checkout"
label = "Rust crate (checkout)"
kind = "rust"
root = "services/checkout"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml", "services/checkout/**/*.rs", "services/checkout/Cargo.toml", "services/checkout/benches/**", "services/checkout/examples/**", "services/checkout/src/**", "services/checkout/tests/**"]
github_pr_commands = ["cd -- 'services/checkout' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/checkout' && mbx nextest run --locked --all-features --package 'checkout' --no-tests pass", "cd -- 'services/checkout' && mbx clippy --locked --profile test --all-targets --all-features --package 'checkout' -- -D warnings"]
github_full_commands = ["cd -- 'services/checkout' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/checkout' && mbx nextest run --locked --all-features --package 'checkout' --no-tests pass", "cd -- 'services/checkout' && mbx clippy --locked --profile test --all-targets --all-features --package 'checkout' -- -D warnings"]
velnor_pr_commands = ["cd -- 'services/checkout' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/checkout' && mbx nextest run --locked --all-features --package 'checkout' --no-tests pass", "cd -- 'services/checkout' && mbx clippy --locked --profile test --all-targets --all-features --package 'checkout' -- -D warnings"]
velnor_full_commands = ["cd -- 'services/checkout' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/checkout' && mbx nextest run --locked --all-features --package 'checkout' --no-tests pass", "cd -- 'services/checkout' && mbx clippy --locked --profile test --all-targets --all-features --package 'checkout' -- -D warnings"]
depends_on = ["rust-playground-proto", "rust-playground-telemetry"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-inventory"
label = "Rust crate (inventory)"
kind = "rust"
root = "services/inventory"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml", "services/inventory/**/*.rs", "services/inventory/Cargo.toml", "services/inventory/benches/**", "services/inventory/examples/**", "services/inventory/src/**", "services/inventory/tests/**"]
github_pr_commands = ["cd -- 'services/inventory' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/inventory' && mbx nextest run --locked --all-features --package 'inventory' --no-tests pass", "cd -- 'services/inventory' && mbx clippy --locked --profile test --all-targets --all-features --package 'inventory' -- -D warnings"]
github_full_commands = ["cd -- 'services/inventory' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/inventory' && mbx nextest run --locked --all-features --package 'inventory' --no-tests pass", "cd -- 'services/inventory' && mbx clippy --locked --profile test --all-targets --all-features --package 'inventory' -- -D warnings"]
velnor_pr_commands = ["cd -- 'services/inventory' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/inventory' && mbx nextest run --locked --all-features --package 'inventory' --no-tests pass", "cd -- 'services/inventory' && mbx clippy --locked --profile test --all-targets --all-features --package 'inventory' -- -D warnings"]
velnor_full_commands = ["cd -- 'services/inventory' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/inventory' && mbx nextest run --locked --all-features --package 'inventory' --no-tests pass", "cd -- 'services/inventory' && mbx clippy --locked --profile test --all-targets --all-features --package 'inventory' -- -D warnings"]
depends_on = ["rust-playground-telemetry"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-notifications"
label = "Rust crate (notifications)"
kind = "rust"
root = "services/notifications"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml", "services/notifications/**/*.rs", "services/notifications/Cargo.toml", "services/notifications/benches/**", "services/notifications/examples/**", "services/notifications/src/**", "services/notifications/tests/**"]
github_pr_commands = ["cd -- 'services/notifications' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/notifications' && mbx nextest run --locked --all-features --package 'notifications' --no-tests pass", "cd -- 'services/notifications' && mbx clippy --locked --profile test --all-targets --all-features --package 'notifications' -- -D warnings"]
github_full_commands = ["cd -- 'services/notifications' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/notifications' && mbx nextest run --locked --all-features --package 'notifications' --no-tests pass", "cd -- 'services/notifications' && mbx clippy --locked --profile test --all-targets --all-features --package 'notifications' -- -D warnings"]
velnor_pr_commands = ["cd -- 'services/notifications' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/notifications' && mbx nextest run --locked --all-features --package 'notifications' --no-tests pass", "cd -- 'services/notifications' && mbx clippy --locked --profile test --all-targets --all-features --package 'notifications' -- -D warnings"]
velnor_full_commands = ["cd -- 'services/notifications' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/notifications' && mbx nextest run --locked --all-features --package 'notifications' --no-tests pass", "cd -- 'services/notifications' && mbx clippy --locked --profile test --all-targets --all-features --package 'notifications' -- -D warnings"]
depends_on = ["rust-playground-telemetry"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-orders"
label = "Rust crate (orders)"
kind = "rust"
root = "services/orders"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml", "services/orders/**/*.rs", "services/orders/Cargo.toml", "services/orders/benches/**", "services/orders/examples/**", "services/orders/src/**", "services/orders/tests/**"]
github_pr_commands = ["cd -- 'services/orders' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/orders' && mbx nextest run --locked --all-features --package 'orders' --no-tests pass", "cd -- 'services/orders' && mbx clippy --locked --profile test --all-targets --all-features --package 'orders' -- -D warnings"]
github_full_commands = ["cd -- 'services/orders' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/orders' && mbx nextest run --locked --all-features --package 'orders' --no-tests pass", "cd -- 'services/orders' && mbx clippy --locked --profile test --all-targets --all-features --package 'orders' -- -D warnings"]
velnor_pr_commands = ["cd -- 'services/orders' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/orders' && mbx nextest run --locked --all-features --package 'orders' --no-tests pass", "cd -- 'services/orders' && mbx clippy --locked --profile test --all-targets --all-features --package 'orders' -- -D warnings"]
velnor_full_commands = ["cd -- 'services/orders' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/orders' && mbx nextest run --locked --all-features --package 'orders' --no-tests pass", "cd -- 'services/orders' && mbx clippy --locked --profile test --all-targets --all-features --package 'orders' -- -D warnings"]
depends_on = ["rust-playground-telemetry"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-playground-cli"
label = "Rust crate (playground-cli)"
kind = "rust"
root = "cli"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "cli/**/*.rs", "cli/Cargo.toml", "cli/benches/**", "cli/examples/**", "cli/src/**", "cli/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
github_pr_commands = ["cd -- 'cli' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'cli' && mbx nextest run --locked --all-features --package 'playground-cli' --no-tests pass", "cd -- 'cli' && mbx clippy --locked --profile test --all-targets --all-features --package 'playground-cli' -- -D warnings"]
github_full_commands = ["cd -- 'cli' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'cli' && mbx nextest run --locked --all-features --package 'playground-cli' --no-tests pass", "cd -- 'cli' && mbx clippy --locked --profile test --all-targets --all-features --package 'playground-cli' -- -D warnings"]
velnor_pr_commands = ["cd -- 'cli' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'cli' && mbx nextest run --locked --all-features --package 'playground-cli' --no-tests pass", "cd -- 'cli' && mbx clippy --locked --profile test --all-targets --all-features --package 'playground-cli' -- -D warnings"]
velnor_full_commands = ["cd -- 'cli' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'cli' && mbx nextest run --locked --all-features --package 'playground-cli' --no-tests pass", "cd -- 'cli' && mbx clippy --locked --profile test --all-targets --all-features --package 'playground-cli' -- -D warnings"]
depends_on = ["rust-playground-telemetry"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-playground-proto"
label = "Rust crate (playground-proto)"
kind = "rust"
root = "proto"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "mise.lock", "mise.toml", "proto/**/*.rs", "proto/Cargo.toml", "proto/benches/**", "proto/build.rs", "proto/examples/**", "proto/src/**", "proto/tests/**", "rust-toolchain", "rust-toolchain.toml"]
github_pr_commands = ["cd -- 'proto' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'proto' && mbx nextest run --locked --all-features --package 'playground-proto' --no-tests pass", "cd -- 'proto' && mbx clippy --locked --profile test --all-targets --all-features --package 'playground-proto' -- -D warnings"]
github_full_commands = ["cd -- 'proto' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'proto' && mbx nextest run --locked --all-features --package 'playground-proto' --no-tests pass", "cd -- 'proto' && mbx clippy --locked --profile test --all-targets --all-features --package 'playground-proto' -- -D warnings"]
velnor_pr_commands = ["cd -- 'proto' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'proto' && mbx nextest run --locked --all-features --package 'playground-proto' --no-tests pass", "cd -- 'proto' && mbx clippy --locked --profile test --all-targets --all-features --package 'playground-proto' -- -D warnings"]
velnor_full_commands = ["cd -- 'proto' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'proto' && mbx nextest run --locked --all-features --package 'playground-proto' --no-tests pass", "cd -- 'proto' && mbx clippy --locked --profile test --all-targets --all-features --package 'playground-proto' -- -D warnings"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-playground-telemetry"
label = "Rust crate (playground-telemetry)"
kind = "rust"
root = "libs/playground-telemetry"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "libs/playground-telemetry/**/*.rs", "libs/playground-telemetry/Cargo.toml", "libs/playground-telemetry/benches/**", "libs/playground-telemetry/examples/**", "libs/playground-telemetry/src/**", "libs/playground-telemetry/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
github_pr_commands = ["cd -- 'libs/playground-telemetry' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'libs/playground-telemetry' && mbx nextest run --locked --all-features --package 'playground-telemetry' --no-tests pass", "cd -- 'libs/playground-telemetry' && mbx clippy --locked --profile test --all-targets --all-features --package 'playground-telemetry' -- -D warnings"]
github_full_commands = ["cd -- 'libs/playground-telemetry' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'libs/playground-telemetry' && mbx nextest run --locked --all-features --package 'playground-telemetry' --no-tests pass", "cd -- 'libs/playground-telemetry' && mbx clippy --locked --profile test --all-targets --all-features --package 'playground-telemetry' -- -D warnings"]
velnor_pr_commands = ["cd -- 'libs/playground-telemetry' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'libs/playground-telemetry' && mbx nextest run --locked --all-features --package 'playground-telemetry' --no-tests pass", "cd -- 'libs/playground-telemetry' && mbx clippy --locked --profile test --all-targets --all-features --package 'playground-telemetry' -- -D warnings"]
velnor_full_commands = ["cd -- 'libs/playground-telemetry' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'libs/playground-telemetry' && mbx nextest run --locked --all-features --package 'playground-telemetry' --no-tests pass", "cd -- 'libs/playground-telemetry' && mbx clippy --locked --profile test --all-targets --all-features --package 'playground-telemetry' -- -D warnings"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-pricing"
label = "Rust crate (pricing)"
kind = "rust"
root = "services/pricing"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml", "services/pricing/**/*.rs", "services/pricing/Cargo.toml", "services/pricing/benches/**", "services/pricing/examples/**", "services/pricing/src/**", "services/pricing/tests/**"]
github_pr_commands = ["cd -- 'services/pricing' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/pricing' && mbx nextest run --locked --all-features --package 'pricing' --no-tests pass", "cd -- 'services/pricing' && mbx clippy --locked --profile test --all-targets --all-features --package 'pricing' -- -D warnings"]
github_full_commands = ["cd -- 'services/pricing' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/pricing' && mbx nextest run --locked --all-features --package 'pricing' --no-tests pass", "cd -- 'services/pricing' && mbx clippy --locked --profile test --all-targets --all-features --package 'pricing' -- -D warnings"]
velnor_pr_commands = ["cd -- 'services/pricing' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/pricing' && mbx nextest run --locked --all-features --package 'pricing' --no-tests pass", "cd -- 'services/pricing' && mbx clippy --locked --profile test --all-targets --all-features --package 'pricing' -- -D warnings"]
velnor_full_commands = ["cd -- 'services/pricing' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/pricing' && mbx nextest run --locked --all-features --package 'pricing' --no-tests pass", "cd -- 'services/pricing' && mbx clippy --locked --profile test --all-targets --all-features --package 'pricing' -- -D warnings"]
depends_on = ["rust-playground-proto", "rust-playground-telemetry"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-recommendation"
label = "Rust crate (recommendation)"
kind = "rust"
root = "services/recommendation"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml", "services/recommendation/**/*.rs", "services/recommendation/Cargo.toml", "services/recommendation/benches/**", "services/recommendation/examples/**", "services/recommendation/src/**", "services/recommendation/tests/**"]
github_pr_commands = ["cd -- 'services/recommendation' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/recommendation' && mbx nextest run --locked --all-features --package 'recommendation' --no-tests pass", "cd -- 'services/recommendation' && mbx clippy --locked --profile test --all-targets --all-features --package 'recommendation' -- -D warnings"]
github_full_commands = ["cd -- 'services/recommendation' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/recommendation' && mbx nextest run --locked --all-features --package 'recommendation' --no-tests pass", "cd -- 'services/recommendation' && mbx clippy --locked --profile test --all-targets --all-features --package 'recommendation' -- -D warnings"]
velnor_pr_commands = ["cd -- 'services/recommendation' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/recommendation' && mbx nextest run --locked --all-features --package 'recommendation' --no-tests pass", "cd -- 'services/recommendation' && mbx clippy --locked --profile test --all-targets --all-features --package 'recommendation' -- -D warnings"]
velnor_full_commands = ["cd -- 'services/recommendation' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/recommendation' && mbx nextest run --locked --all-features --package 'recommendation' --no-tests pass", "cd -- 'services/recommendation' && mbx clippy --locked --profile test --all-targets --all-features --package 'recommendation' -- -D warnings"]
depends_on = ["rust-playground-telemetry"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-storefront"
label = "Rust crate (storefront)"
kind = "rust"
root = "services/storefront"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml", "services/storefront/**/*.rs", "services/storefront/Cargo.toml", "services/storefront/benches/**", "services/storefront/examples/**", "services/storefront/src/**", "services/storefront/tests/**"]
github_pr_commands = ["cd -- 'services/storefront' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/storefront' && mbx nextest run --locked --all-features --package 'storefront' --no-tests pass", "cd -- 'services/storefront' && mbx clippy --locked --profile test --all-targets --all-features --package 'storefront' -- -D warnings"]
github_full_commands = ["cd -- 'services/storefront' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/storefront' && mbx nextest run --locked --all-features --package 'storefront' --no-tests pass", "cd -- 'services/storefront' && mbx clippy --locked --profile test --all-targets --all-features --package 'storefront' -- -D warnings"]
velnor_pr_commands = ["cd -- 'services/storefront' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/storefront' && mbx nextest run --locked --all-features --package 'storefront' --no-tests pass", "cd -- 'services/storefront' && mbx clippy --locked --profile test --all-targets --all-features --package 'storefront' -- -D warnings"]
velnor_full_commands = ["cd -- 'services/storefront' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'services/storefront' && mbx nextest run --locked --all-features --package 'storefront' --no-tests pass", "cd -- 'services/storefront' && mbx clippy --locked --profile test --all-targets --all-features --package 'storefront' -- -D warnings"]
depends_on = ["rust-playground-proto", "rust-playground-telemetry"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "swift-package-macos"
label = "Swift package (macos)"
kind = "swift"
root = "macos"
watch = ["macos/**/*.swift", "macos/.swiftpm/Package.resolved", "macos/Package.resolved", "macos/Package.swift", "macos/Sources/**", "macos/Tests/**"]
github_pr_commands = ["cd -- 'macos' && swift build", "cd -- 'macos' && swift test --parallel"]
github_full_commands = ["cd -- 'macos' && swift build", "cd -- 'macos' && swift test --parallel"]
velnor_pr_commands = ["cd -- 'macos' && swift build", "cd -- 'macos' && swift test --parallel"]
velnor_full_commands = ["cd -- 'macos' && swift build", "cd -- 'macos' && swift test --parallel"]
[unit.cache]
key_files = ["macos/Package.swift", "macos/Package.resolved", "macos/.swiftpm/Package.resolved"]
paths = ["~/.swiftpm"]
