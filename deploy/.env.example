# Copied to deploy/.env (gitignored). Compose interpolates GIT_SHA into every
# service so `vcs.ref.head.revision` is never empty.
#   git rev-parse HEAD | awk '{print "GIT_SHA="$1}' > deploy/.env
GIT_SHA=
